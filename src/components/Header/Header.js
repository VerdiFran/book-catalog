import React from'react'
import {Layout} from 'antd'

/**
 * Header component
 * @returns {JSX.Element}
 * @constructor
 */
const Header = () => {
    const {Header} = Layout

    return (
        <Header>
            Book Catalog
        </Header>
    )
}

export default Header
